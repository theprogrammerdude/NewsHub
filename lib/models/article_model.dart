class ArticleModel {
  String title, description, url, urlToImage, content;

  // DateTime publishedAt;

  ArticleModel({
    this.title,
    this.description,
    this.url,
    this.urlToImage,
    this.content,
    // this.publishedAt
  });
}
